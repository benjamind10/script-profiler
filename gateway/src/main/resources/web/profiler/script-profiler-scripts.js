(function() {
  const root = document.getElementById("script-profiler-root");
  if (root) {
    root.innerHTML = `
      <div style="font-family: sans-serif; font-size: 16px; padding: 2em;">
        🧪 <strong>Script Profiler React Config</strong><br/>
        This is a placeholder React page mounted inside the Gateway config panel.
      </div>
    `;
  }
})();
